# PAM monorepo — combined requirements for a single shared venv.
# Install with: pip install -r requirements.txt
# Or run:       ./setup-venv.sh

# shared package (editable so all services pick up local changes)
-e ./shared

# web / API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# databases & messaging
motor>=3.5.0
redis[asyncio]>=5.0.0
clickhouse-connect>=0.7.0
aiokafka>=0.10.0
aiomysql>=0.2.0

# config & validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# observability
structlog>=24.2.0

# MCP (wynta-mcp)
mcp[cli]>=1.0.0
httpx>=0.27.0

# dev / test / lint
pytest>=8.2.0
pytest-asyncio>=0.23.0
ruff>=0.4.0
mypy>=1.10.0
