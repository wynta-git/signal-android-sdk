[project]
name = "pam-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "motor>=3.5.0",
    "redis[asyncio]>=5.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
