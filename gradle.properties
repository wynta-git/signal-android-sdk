org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# ── SDK coordinates ───────────────────────────────────────────────────────────
GROUP=io.github.wynta-git
VERSION_NAME=1.4.4

# ── Signing (local dev only — use env vars in CI) ─────────────────────────────
# signing.keyId=<last 8 chars of GPG key ID>
# signing.key=<ASCII-armored GPG private key>
# signing.password=<key passphrase>

# ── OSSRH / Central Portal credentials (local dev only — use env vars in CI) ──
# ossrhUsername=<Sonatype username or token>
# ossrhPassword=<Sonatype password or token>
