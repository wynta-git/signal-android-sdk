[project]
name = "scheduler-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "motor>=3.5.0",
    "redis[asyncio]>=5.0.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0",
    "structlog>=24.2.0",
    "aiokafka>=0.10.0",
    "croniter>=2.0.0",
    "httpx>=0.27.0",
    "pam-shared",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
]

[tool.uv.sources]
pam-shared = { path = "../../shared", editable = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
strict = true
python_version = "3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
